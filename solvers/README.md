# Solvers

Each solver implementation belongs in its own subdirectory here.

Solver directories should expose a `sparse_solver_interface` plugin and keep
solver-specific dependency discovery local to that directory.
